@model IEnumerable<Hiriart_Corales_MVCWebApp_AgendaPersonal.Models.Notificacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm())
{
    @Html.TextBox("Titulo")
    <input type="submit" value="Buscar" />
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Evento.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hora)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Evento.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hora)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.NotificacionID }) |
                @Html.ActionLink("Details", "Details", new { id = item.NotificacionID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.NotificacionID })
            </td>
        </tr>
    }

</table>
